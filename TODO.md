# TODO

- [ ] Write `sync-claude-permissions.sh` — reads `allowed-commands.txt` and regenerates the `permissions.allow` block in `~/.claude/settings.json`
- [ ] Wire sync script into a broader setup/install script for fresh machine provisioning
